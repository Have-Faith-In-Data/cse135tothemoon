$( document ).ready(function() {
    
// TODO: not really good looking, need to modify, unused right now
//  let github_info = {
//      'username': 'shikanchen',
//      'repos' : ['language-modeling-cometh', '2048AI', 'Terminal', 'GraphModel']
//  };
//  
//  for (let i = 0; i < github_info['repos'].length; i++) {
//      $( "#repos" ).append('<object class="repo" type="image/svg+xml" data=""></object>')
//  }
//  
//  $( ".repo" ).each(function( index ) {
//      console.log( index + ": " + $( this ).text() );
//      $( this ).attr("data","https://gh-card.dev/repos/"+github_info['username']+"/"+github_info['repos'][index]+".svg?fullname=&link_target=_blank");
//  });
});